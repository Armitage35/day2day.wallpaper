import React from 'react';
import Toolbar from './Toolbar/Toolbar.js';

const Layout = (props) => {
    return (
        <div>
            <p>Layout</p>
            <Toolbar />
        </div>
    );
};

export default Layout;
